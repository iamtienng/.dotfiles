-- PARKED REFERENCE — not loaded by Hyprland. Translation of ../looknfeel.conf.
-- (The commented-out animations{} / layout{} blocks in the .conf are omitted.)
--
-- [OK] hl.config({ section = { key = value } }) is verbatim-confirmed.

hl.config({
  general = {
    gaps_in = 0,
    gaps_out = 0,
    border_size = 0,
  },
  dwindle = {
    preserve_split = true,
    force_split = 0,
  },
  decoration = {
    rounding = 8,
  },
})
